'use strict';

/**
 * bewegwijzering controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::bewegwijzering.bewegwijzering');
